site:
  title: "Zenmo Web"

server:
  cors:
    hosts:
      -   name: "preview.zenmo.com"
      -   name: "preview.lux.energy"
  files:
    dev:
      contentRoot: "build/processedResources/js/main/public"
      script: "build/rollup/zenmo-site.mjs"
      api: "build/libs/zenmo.jar"
    prod:
      script: "build/rollup/zenmo-site.mjs"
      siteRoot: ".kobweb/site"

  port: 8080
